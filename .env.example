# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/mahogany_games"

# Optional: PostgreSQL connection details (if not using DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=mahogany_games

# Frontend Environment Variables (must be prefixed with VITE_)
VITE_GA_MEASUREMENT_ID=your_google_analytics_id

# Development Configuration
NODE_ENV=development
PORT=5000

# Session Configuration
SESSION_SECRET=your_super_secret_session_key_here

# Optional: Email Configuration (for newsletter)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_email_password